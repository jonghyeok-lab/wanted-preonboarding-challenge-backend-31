spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/cqrs_db
    username: user
    password: test
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.wanted.cqrs: debug
    sql: debug

---

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cqrs_test_db
    username: user
    password: test
  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
  jackson:
    property-naming-strategy: SNAKE_CASE